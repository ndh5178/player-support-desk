# Player Support Desk 작업 지침

이 파일은 `player-support-desk` 저장소 전체에 적용됩니다.

## 작업 전 확인

코드를 변경하기 전에 다음 문서를 순서대로 확인합니다.

1. `docs/PROJECT_SPEC.md`
2. `docs/IMPLEMENTATION_PLAN.md`
3. `docs/WORK_UNITS.md`
4. `docs/PROJECT_STRUCTURE.md`
5. `docs/DECISIONS.md`
6. `docs/GIT_WORKFLOW.md`
7. 변경 범위와 관련된 `docs/LEARNING_NOTES.md`, `docs/QA_CHECKLIST.md`

문서와 구현이 다르면 임의로 둘 중 하나를 무시하지 않습니다. 사용자의 최신 지시를 우선하고, 확정된 변경 사항을 관련 문서에 함께 반영합니다.

## 프로젝트 목적

- Vue 3와 TypeScript를 학습해 완성한 결과물을 사실 기반으로 보여주는 포트폴리오입니다.
- 글로벌 게임 서비스 운영자가 고객 문의를 빠르게 파악하고 처리하는 흐름을 구현합니다.
- 장식이나 불필요한 추상화보다 정보 구조, 사용성, 설명 가능한 코드를 우선합니다.

## 작업 범위와 금지 사항

- 모든 프로젝트 파일은 이 저장소 안에서만 생성하거나 수정합니다.
- 상위 폴더의 기존 이력서 `C:\Users\ehdgu\Desktop\정글\index.html`은 수정하지 않습니다.
- 실제 회사, 게임, 로고, 캐릭터, 이미지 또는 데이터를 사용하지 않습니다.
- 별도의 복잡한 백엔드나 실제 데이터베이스를 만들지 않습니다.
- 배포, 학습 기간, 성능 수치 등 확인되지 않은 내용을 README에 사실처럼 쓰지 않습니다.
- 비밀키, 토큰, 개인 데이터와 로컬 전용 설정을 커밋하지 않습니다.

## 구현 원칙

- Vue 3 Composition API와 `<script setup lang="ts">`를 기본으로 사용합니다.
- 컴포넌트는 화면에 이름을 맞추기 위해 억지로 분리하지 않고, 하나의 명확한 책임이 있을 때 분리합니다.
- 부모·자식 UI 통신에는 Props/Emit을 사용하고, 여러 화면에서 공유되는 서버 상태만 Pinia에서 관리합니다.
- 입력값과 펼침 상태처럼 한 컴포넌트에서만 쓰는 값은 로컬 `ref`로 둡니다.
- 파생 값은 `computed`, 필요한 외부 동기화는 `watch`를 사용합니다.
- API 접근은 `src/services/api.ts`를 통하게 하여 MSW를 실제 서버로 교체하기 쉽게 유지합니다.
- 요청 경합이 가능한 조회에는 AbortController 또는 동등한 취소 처리를 적용합니다.
- 상세 라우트는 Lazy Loading합니다.
- 구조가 실제로 복잡해질 때만 `services`, `stores`, `types`, `composables` 파일을 추가로 분리합니다.

## UI와 품질 원칙

- 데스크톱, 태블릿, 모바일 레이아웃을 CSS 미디어 쿼리로 직접 구현합니다.
- 레이아웃을 위해 불필요한 JavaScript `resize` 이벤트를 사용하지 않습니다.
- 모바일에서 테이블 대신 카드 목록을 제공하고 가로 스크롤에 의존하지 않습니다.
- 로딩, 오류, 재시도, 검색 결과 없음, 필터 결과 없음, 존재하지 않는 문의를 명시적으로 처리합니다.
- 상태와 우선순위는 색상뿐 아니라 텍스트로도 전달합니다.
- 시맨틱 HTML, 명확한 label, 키보드 포커스, `aria-live`, `prefers-reduced-motion`을 확인합니다.

## 검증 원칙

- 기능 단위로 관련 테스트를 먼저 실행합니다.
- 병합 전에는 TypeScript, ESLint, Vitest, 프로덕션 빌드를 모두 통과시킵니다.
- 주요 세 화면을 데스크톱, 태블릿, 모바일 크기에서 직접 확인합니다.
- 실행한 검증과 발견한 제한 사항을 `docs/QA_CHECKLIST.md`에 반영합니다.
- 테스트를 통과시키기 위해 요구사항을 약화하거나 의미 있는 검증을 삭제하지 않습니다.

## 문서 관리

- 사용자 요구사항이 바뀌면 `docs/PROJECT_SPEC.md`를 갱신합니다.
- 작업 순서나 완료 상태가 바뀌면 `docs/IMPLEMENTATION_PLAN.md`를 갱신합니다.
- 기술 선택이나 구조가 달라지면 `docs/DECISIONS.md`에 이유를 기록합니다.
- Vue 개념을 실제 코드에 적용하면 `docs/LEARNING_NOTES.md`에 파일과 사례를 연결합니다.
- 외부 공개용 `README.md`는 검증된 사실만 사용하고 내부 작업 메모를 그대로 옮기지 않습니다.

## Git

- 모든 Git 작업은 `docs/GIT_WORKFLOW.md`를 따릅니다.
- `main`에 기능 코드를 직접 커밋하지 않습니다.
- 커밋 하나에는 하나의 설명 가능한 목적만 담습니다.
- 커밋에는 Conventional Commits 형식의 제목과 변경 이유·핵심 내용을 설명하는 본문을 함께 작성합니다.
- 사용자 변경과 관련 없는 파일은 스테이징하거나 되돌리지 않습니다.
- 공개된 이력을 임의로 조작하거나 기여를 과장하지 않습니다.
